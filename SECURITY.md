# 安全说明

## 报告漏洞

请勿在公开 Issue 中发布有效凭证、可被直接利用的部署地址或尚未修复漏洞的完整利用步骤。

优先使用仓库 Security 页的 **Report a vulnerability** 私密报告入口（需要维护者开启）。如该入口尚未启用，可先提交不含漏洞细节和敏感信息的 Issue，请维护者提供私密联系方式，再交换详情。此项目尚未承诺固定的响应时限。

报告应包含受影响版本、最小复现、预期与实际结果。使用模拟数据和已脱敏的日志；如凭证已经暴露，应由部署者及时吊销或轮换。

## 当前边界

- 面板管理员拥有配置上游地址、管理员 Key 和主动探测目标的权限。这些配置会发起服务端网络请求；管理员权限本身属于高信任边界。
- 上游地址和探测目标需要来自可信管理员。若需要限制内网访问范围，请同时使用网络出口策略；应用没有通用的 SSRF 地址白名单。
- 公开面板默认展示部署者选定分组的聚合指标。首次连接或更换站点后需选择分组并保存；管理设置、系统采样和探测历史仅供管理员使用。
- 源站 Key 经 AES-256-GCM 加密后存储。密钥与密文位于同一私有数据目录，读取整个目录的攻击者仍可解密。文件权限和备份权限是必要边界。
- 会话保存在内存中；重启后需要重新登录。当前仅支持单进程、单实例写同一数据目录，不支持共享卷上的多实例部署。
- HTTP 写请求校验 Origin 和 JSON 类型，登录与初始化受限速约束。生产应启用 HTTPS，并正确配置 `KANBAN_PUBLIC_ORIGIN`。
- 监控采集有缓存容量、并发、排队及总时限限制；高流量公开部署仍需反向代理的连接数和请求速率限制。
